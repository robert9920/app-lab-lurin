# El navegador usa /api; no se requiere ninguna clave VITE_.
# Variables del servidor App Service (no se incluyen en el bundle):
API_TARGET=https://YOUR-FUNCTION.azurewebsites.net
FUNCTION_PROXY_KEY=SERVER_SIDE_FUNCTION_KEY
PORT=8080
NODE_ENV=production
